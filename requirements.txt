# CourierAI - requirements.txt unificado
# Instalar con: pip install -r requirements.txt --break-system-packages
# (o sin la bandera si no estás en un entorno gestionado tipo Linux/WSL)

# --- Backend / APIs ---
fastapi
uvicorn
pydantic
google-genai
requests>=2.31

# --- Motor matemático / optimización ---
ortools>=9.9
scikit-learn>=1.3

# --- Rutas / mapas / geodata ---
osmnx>=1.9.0
networkx>=3.0
folium>=0.16.0

# --- Datos ---
pandas>=2.0.0
python-dotenv>=1.0.0
psycopg2-binary>=2.9.9

# --- Frontend ---
streamlit
streamlit-folium>=0.20